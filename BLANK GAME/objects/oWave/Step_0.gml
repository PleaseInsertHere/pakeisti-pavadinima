y = startY + (sin(current_time/500) * waveHeight)

image_angle = direction;

y = startY + sin(yInputStart /500); // play around with the dividing number to fit your game

yInputStart = yInputStart + 1; // to update this value for movement "manually"